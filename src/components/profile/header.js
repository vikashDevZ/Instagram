/* eslint-disable */

import React from "react";

const Header = ({
  photosCount,
  profile,
  followersCount,
  setFollowersCount,
}) => {
  console.log("photosCount", props);
  return <div>Header</div>;
};

export default Header;
